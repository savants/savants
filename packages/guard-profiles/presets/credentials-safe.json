[
  "when tool eq 'Read' and file_path contains '/etc/passwd' then ask 'Reading system password file'",
  "when tool eq 'Read' and file_path contains '/etc/shadow' then block",
  "when tool eq 'Read' and file_path contains '/etc/sudoers' then ask 'Reading sudoers configuration'",
  "when tool eq 'Write' and file_path contains '/etc/passwd' then block",
  "when tool eq 'Write' and file_path contains '/etc/shadow' then block",
  "when tool eq 'Write' and file_path contains '/etc/sudoers' then block",
  "when tool eq 'Edit' and file_path contains '/etc/passwd' then block",
  "when tool eq 'Edit' and file_path contains '/etc/shadow' then block",
  "when tool eq 'Edit' and file_path contains '/etc/sudoers' then block",
  "when tool eq 'Read' and file_path contains '.ssh/id_' then block",
  "when tool eq 'Read' and file_path contains '.ssh/authorized_keys' then ask 'Reading SSH authorized_keys'",
  "when tool eq 'Read' and file_path contains '.ssh/config' then ask 'Reading SSH config may reveal host details'",
  "when tool eq 'Write' and file_path contains '.ssh' then block",
  "when tool eq 'Edit' and file_path contains '.ssh' then block",
  "when tool eq 'Read' and file_path contains '.aws/credentials' then block",
  "when tool eq 'Write' and file_path contains '.aws/credentials' then block",
  "when tool eq 'Read' and file_path contains '.gcloud/' then block",
  "when tool eq 'Write' and file_path contains '.gcloud/' then block",
  "when tool eq 'Read' and file_path contains '.kube/config' then ask 'Reading kubeconfig may expose cluster credentials'",
  "when tool eq 'Write' and file_path contains '.kube/config' then block",
  "when tool eq 'Write' and file_path contains '.env' then ask 'Writing to .env may expose secrets if committed'",
  "when tool eq 'Edit' and file_path contains '.env' then ask 'Editing .env may expose secrets if committed'",
  "when tool eq 'Read' and file_path contains '.env' then ask 'Reading .env may expose secrets to the agent context'",
  "when tool eq 'Write' and file_path ends_with '.pem' then block",
  "when tool eq 'Write' and file_path ends_with '.key' then block",
  "when tool eq 'Write' and file_path ends_with '.p12' then block",
  "when tool eq 'Write' and file_path ends_with '.pfx' then block",
  "when tool eq 'Read' and file_path contains '.bash_history' then block",
  "when tool eq 'Read' and file_path contains '.zsh_history' then block",
  "when tool eq 'Read' and file_path contains '/proc/self/environ' then block",
  "when tool eq 'Write' and content contains 'sk_live_' then suggest 'Use an environment variable instead of hardcoding Stripe live keys'",
  "when tool eq 'Write' and content contains 'sk-ant-' then suggest 'Use an environment variable instead of hardcoding Anthropic API keys'",
  "when tool eq 'Write' and content contains 'ghp_' then suggest 'Use an environment variable instead of hardcoding GitHub personal tokens'",
  "when tool eq 'Write' and content contains 'gho_' then suggest 'Use an environment variable instead of hardcoding GitHub OAuth tokens'",
  "when tool eq 'Write' and content contains 'AKIA' then suggest 'Use an environment variable instead of hardcoding AWS access keys'",
  "when tool eq 'Write' and content contains 'xoxb-' then suggest 'Use an environment variable instead of hardcoding Slack bot tokens'",
  "when tool eq 'Write' and content contains 'xoxp-' then suggest 'Use an environment variable instead of hardcoding Slack user tokens'",
  "when tool eq 'Write' and content contains 'xoxs-' then suggest 'Use an environment variable instead of hardcoding Slack tokens'",
  "when tool eq 'Edit' and new_string contains 'sk_live_' then suggest 'Use an environment variable instead of hardcoding Stripe live keys'",
  "when tool eq 'Edit' and new_string contains 'sk-ant-' then suggest 'Use an environment variable instead of hardcoding Anthropic API keys'",
  "when tool eq 'Edit' and new_string contains 'ghp_' then suggest 'Use an environment variable instead of hardcoding GitHub personal tokens'",
  "when tool eq 'Edit' and new_string contains 'AKIA' then suggest 'Use an environment variable instead of hardcoding AWS access keys'",
  "when tool eq 'Write' and content contains 'BEGIN RSA PRIVATE KEY' then block",
  "when tool eq 'Write' and content contains 'BEGIN PRIVATE KEY' then block",
  "when tool eq 'Write' and content contains 'BEGIN EC PRIVATE KEY' then block",
  "when tool eq 'Edit' and new_string contains 'BEGIN RSA PRIVATE KEY' then block",
  "when tool eq 'Edit' and new_string contains 'BEGIN PRIVATE KEY' then block",
  "when tool eq 'Bash' and command contains 'printenv' then ask 'Printing environment variables may expose secrets'",
  "when tool eq 'Bash' and command contains 'env |' then ask 'Piping env output may expose secrets to other commands'",
  "when tool eq 'Bash' and command contains 'env >' then ask 'Dumping environment variables to a file may expose secrets'",
  "when tool eq 'Bash' and command contains 'cat /proc/self/environ' then block",
  "when tool eq 'Bash' and command contains 'echo $SECRET' then block",
  "when tool eq 'Bash' and command contains 'echo $TOKEN' then block",
  "when tool eq 'Bash' and command contains 'echo $PASSWORD' then block",
  "when tool eq 'Bash' and command contains 'echo $API_KEY' then block",
  "when tool eq 'Bash' and command contains 'echo $AWS_SECRET' then block",
  "when tool eq 'Bash' and command contains 'cat' and command contains 'id_rsa' then block",
  "when tool eq 'Bash' and command contains 'pass ' then ask 'Accessing password store — verify this is needed'"
]
